name: Sync upstream and build image

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'Dockerfile'
      - '.github/workflows/sync-upstream.yml'
      - 'docs/**'
      - 'scripts/**'
      - 'README.md'
  pull_request:
    branches:
      - main
      - master
  schedule:
    # Every day at 03:30 UTC
    - cron: '30 3 * * *'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR (optional)
        if: ${{ secrets.GHCR_PAT != '' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Login to Docker Hub (only when pushing)
        if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build (push disabled on push/PR)
        uses: docker/build-push-action@v6
        env:
          OPENCLAW_GH_TOKEN: ${{ secrets.OPENCLAW_GH_TOKEN }}
        with:
          context: .
          # Avoid failing runs when Docker Hub credentials/repo permissions are not ready.
          # We only push on schedule/workflow_dispatch.
          push: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}
          tags: deltrivx/openclaw:latest

          cache-from: type=gha
          cache-to: type=gha,mode=max
