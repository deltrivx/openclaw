# Start from a strict deny-all to avoid accidental huge contexts,
# then explicitly allow what the Dockerfile needs.
**

# Always include Docker build files
!Dockerfile
!.dockerignore

# Include repo-local translation assets
!translations/**
!scripts/**
!patches/**
